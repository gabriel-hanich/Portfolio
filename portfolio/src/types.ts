export interface Experience{
    name: string;
    svg: string;
    width: string;
    startDate: Date
    duration: string;
}

export interface Project{
    id: string;
    name: string;
    description: string;
    excerpt: string;
    importantRank: number;
}